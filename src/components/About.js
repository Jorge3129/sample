import React, {useState} from 'react';

const About = () => {

    const [val, setVal] = useState(() => {})

    return (
        <main>
            <h2>About</h2>
            Lorem ipsum:)
        </main>
    );
};

export default About;
